import { RoleClassPipe } from './role-class-pipe';

describe('RoleClassPipe', () => {
  it('create an instance', () => {
    const pipe = new RoleClassPipe();
    expect(pipe).toBeTruthy();
  });
});
